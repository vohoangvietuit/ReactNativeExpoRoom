/**
 * Re-export from @fitsync/ui — single source of truth.
 * Local import path preserved for existing consumers.
 */
export { ThemedView } from '@fitsync/ui';
export type { ThemedViewProps } from '@fitsync/ui';
