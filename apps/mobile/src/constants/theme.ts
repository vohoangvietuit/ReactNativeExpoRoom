/**
 * Theme re-exports from @fitsync/ui — single source of truth.
 * Import from '@/constants/theme' in app code (unchanged API).
 */

export { Colors, type ColorScheme, type ThemeColor } from '@fitsync/ui';
export { Fonts, FontSize } from '@fitsync/ui';
export { Spacing, BorderRadius, BottomTabInset, MaxContentWidth } from '@fitsync/ui';
